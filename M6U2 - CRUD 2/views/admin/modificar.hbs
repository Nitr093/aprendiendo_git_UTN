<div class="container" style="margin: 50px auto">
    <div class="row">
        <div class="col-6 offset-3">
            <h3>Modificar la novedad N° {{novedad.id}}</h3>
            <p class="fst-italic">(Puede reemplazar la imagen actual o bien eliminarla)</p>
            <form action="/admin/novedades/modificar" method="post" enctype="multipart/form-data">
                <input type="hidden" value="{{novedad.id}}" name="id">

                <div class="form-group mb-3 row">
                    <input type="text" placeholder="Titulo" value="{{novedad.titulo}}" class="form-control"
                        name="titulo">
                </div>

                <div class="form-group mb-3 row">
                    <textarea placeholder="Subtitulo" class="form-control" name="subtitulo">{{novedad.titulo}}
                    </textarea>
                </div>

                <div class="form-group mb-3 row">
                    <textarea placeholder="Cuerpo" class="form-control" name="cuerpo">{{novedad.cuerpo}}
                    </textarea>
                </div>

                <div class="form-group mb-3 row">
                    <label for="formFile" class="form-label fs-5 text">Subir Imagen <input class="form-control mt-1" type="file" name="imagen" id="imagen formFile" accept="image/png, image/jpeg, image/jpg" /></label>
                    <label class="fs-7 text"><input type="checkbox" name="img_delete" id="img_delete" value="1" /> Borrar Imagen</label>
                    <input type="hidden" name="img_original" value="{{novedad.img_id}}" />
                </div>

                <hr>
                <button type="submit" class="btn btn-primary mt-2">Modificar <i
                        class="fa-solid fa-pen-to-square"></i></button>
                <a class="btn btn-primary mt-2" href='/admin/novedades'><i class="fa-solid fa-left-long"></i> Volver</a>


            </form>
            {{#if error}}
            <p>{{message}}</p>
            {{/if}}


        </div>
    </div>
</div>